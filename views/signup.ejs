<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>RECIPE JOIN</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap-theme.min.css">
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>
    </head>
    <style>
        .headForm{margin-top: 8%;}
        body{background-color: #e9ecef;}
    </style>
    <body>
        <%
			if (status == "id"){
	  	%>
		<script>
			alert("ID를 입력하세요.");
		</script>
		<%
            } else if (status == "pw") {
        %>
        <script>
            alert("Password를 입력하세요.");
        </script>
        <%
            } else if (status == "pw") {
        %>
        <script>
            alert("Password를 다시 입력해주세요.");
        </script>
        <%
            } else if (status == "email") {
        %>
        <script>
            alert("E-Mail를 입력하세요.");
        </script>
        <%
			} else if (status == "name") {
		%>
		<script>
			alert("이름을 입력하세요.");
		</script>
		<%
			} else if (status == "phone") {
		%>
		<script>
			alert("전화번호를 입력하세요.");
		</script>
		<%
			}
        %>
        
        <article class="container">
            <div class="headForm">
                <div class="col-md-6 col-md-offset-3">
                        <h3 style="text-align: center;font-weight: bold;">혼달 회원가입 Form</h3>
                </div>
            </div>
            <div class="col-sm-6 col-md-offset-3">
                  
                <form role="form" action="/signup" method="post"> 
                    <div class="form-group">
                        <label for="inputID">아이디</label>
                        <input type="text" class="form-control" name="inputID" id="inputID" placeholder="이름을 입력해 주세요" value=>
                    </div>
                    <div class="form-group">
                        <label for="InputPW">비밀번호</label>
                        <input type="password" class="form-control" name="InputPW" id="InputPW" placeholder="10자~12자리의 영문(대소문자)+숫자+특수문자 중 2종류 이상을 조합">
                    </div> 
                    <div class="form-group">
                        <label for="inputName">이름</label>
                        <input type="text" class="form-control" name="inputName"  id="inputName" placeholder="이름을 입력해주세요">
                    </div>
                    <div class="form-group">
                        <label for="InputEmail">이메일</label>
                        <input type="email" class="form-control" name="InputEmail" id="InputEmail" placeholder="이메일을  입력해주세요">
                    </div>
                    <div class="form-group">
                        <label for="inputMobile">휴대폰 번호</label>
                        <input type="tel" class="form-control"  name="inputMobile" id="inputMobile" placeholder="휴대폰번호를 입력해 주세요">
                    </div>
                    <div class="form-group text-center">
                        <button type="submit" id="join-submit" class="btn btn-primary" onclick="chekPassword()">
                            회원가입<i class="fa fa-check spaceLeft"></i>
                        </button>
                    </div>
                </form>
            </div>

        </article>
    </body>
</html>

